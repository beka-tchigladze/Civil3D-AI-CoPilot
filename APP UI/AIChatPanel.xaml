<UserControl x:Class="Cad_AI_Agent.UI.AIChatPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500"
             Background="#222222" FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="SidebarButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" x:Name="SidebarColumn"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="10,0" BorderBrush="#333333" BorderThickness="0,0,0,1">
                    <Grid>
                        <TextBlock Text="Chat History" Foreground="#D4D4D4" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Button x:Name="SidebarNewChatBtn" Content="➕" Background="Transparent" Foreground="#0696D7" BorderThickness="0" 
                                FontSize="14" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Center" Click="ClearChatButton_Click" ToolTip="Start New Chat"/>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="HistoryListPanel" Margin="5"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#2D2D30" Cursor="SizeWE"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="0,0,0,1">
                <Grid Margin="15,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                        <TextBlock Text="🤖" FontSize="22" Foreground="#0696D7" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="AI CAD Agent" Foreground="#0696D7" FontSize="17" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="TabChatButton" Content="Chat" Background="Transparent" Foreground="#0696D7" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="TabChatButton_Click" FontWeight="Bold"/>
                        <Button x:Name="TabSettingsButton" Content="⚙️ Settings" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Cursor="Hand" Click="TabSettingsButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid x:Name="ChatTab" Grid.Row="1" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="10,10,10,10">
                    <StackPanel x:Name="ChatHistoryPanel" VerticalAlignment="Bottom"/>
                </ScrollViewer>

                <Border Grid.Row="1" Background="#2D2D30" BorderBrush="#333333" BorderThickness="0,1,0,0" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="UserInputBox" Grid.Column="0" Background="#1E1E1E" Foreground="#D4D4D4" 
                                 CaretBrush="#D4D4D4" BorderThickness="1" BorderBrush="#3F3F46" Padding="10,8" 
                                 FontSize="13" TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="100"
                                 VerticalScrollBarVisibility="Auto" KeyDown="UserInputBox_KeyDown"/>

                        <Button x:Name="SendButton" Grid.Column="1" Margin="10,0,0,0" Width="45" Height="35"
                                Background="#0696D7" Foreground="White" BorderThickness="0" FontWeight="Bold"
                                Content="➤" FontSize="16" Cursor="Hand" Click="SendButton_Click" VerticalAlignment="Bottom">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>
            </Grid>

            <Grid x:Name="SettingsTab" Grid.Row="1" Visibility="Collapsed" Margin="20">
                <StackPanel>
                    <TextBlock Text="AI Provider" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                    <ComboBox x:Name="ProviderCombo" Background="#1E1E1E" Foreground="#D4D4D4" BorderBrush="#3F3F46" Padding="5" Margin="0,0,0,15">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <ToggleButton IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                                              Background="#1E1E1E" BorderBrush="#3F3F46" Foreground="#D4D4D4">
                                                    <ContentPresenter Margin="10,3,25,3" Content="{TemplateBinding SelectionBoxItem}" VerticalAlignment="Center"/>
                                                </ToggleButton>
                                                <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom">
                                                    <Border Background="#1E1E1E" BorderThickness="1" BorderBrush="#3F3F46">
                                                        <ItemsPresenter />
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Style>

                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#D4D4D4"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#333333"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#0696D7"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Foreground" Value="#777777"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.ItemContainerStyle>

                        <ComboBoxItem Content="Google Gemini (2.5 Flash)" Tag="gemini-2.5-flash" IsSelected="True"/>
                        <ComboBoxItem Content="Google Gemini (3.1 Pro)" Tag="gemini-3.1-pro"/>
                        <ComboBoxItem Content="OpenAI (ChatGPT) - Coming Soon" IsEnabled="False"/>
                    </ComboBox>

                    <TextBlock Text="API Key" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                    <TextBox x:Name="ApiKeyBox" Background="#1E1E1E" Foreground="#D4D4D4" BorderBrush="#3F3F46" Padding="8" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="TestConnectionBtn" Grid.Column="0" Content="Test Connection" Background="#333333" Foreground="White" BorderThickness="0" Padding="10,5" Cursor="Hand" Click="TestConnectionBtn_Click"/>
                        <TextBlock x:Name="ConnectionStatusText" Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="14"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>